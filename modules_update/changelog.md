### v1.6.3更新日志
- 主程序：修复规则修改后不会实时生效的BUG

### v1.6.2更新日志
- 主程序：改进扫描逻辑，修复部分机型规则不生效的BUG
- 模块：增加了部分默认规则（默认规则仅在首次刷入模块时增加，默认规则全机型通用，由模块自动选择本机合适的CPU簇）
- 包含了微信、QQ、淘宝、抖音、支付宝、高德地图、酷安、Android系统关键组件、王者荣耀、和平精英、原神、三角洲行动等应用
- 模块：重写了customize.sh中的format_cpu_ranges函数，执行速度更快输出结果也更准确

### v1.5.6更新日志
- 主程序：性能优化，引入PID过滤和跟踪机制，减少90%+的cmdline扫描量。
- 主程序：内存管理优化，通过realloc复用内存，减少内存碎片，改用calloc初始化避免未定义行为。
- 主程序：优化多线程同步，添加内存序控制，以避免数据竞争问题。
- 主程序：修正了1.3.18中的诸多错误。
- 模块：customize.sh增加format_cpu_ranges函数与相关脚本，实现将连续 CPU ID 范围合并与排序，例如0,1,2,3,4合并为0-4或者0,1,2,4合并为0-2,4，为后续实现一个模块同时兼容多种CPU规格的实现做准备。

### v1.3.18更新日志
- 主程序：重构AppOpt为多线程架构
- 主程序：新增"ConfigLoader"线程，负责配置加载
- 主程序：改用inotify实时监控配置文件变化，从轮询改为事件驱动，空闲0占用
- 主程序：当设备不支持inotify或监控失败，则自动回退到轮询机制
- 主程序：实现了主线程/加载器线程通过原子变量同步

### v1.3.5更新日志
- 主程序：性能优化，核心绑定时间更快的同时，程序自身开销更低
- 主程序：修复了一些BUG，其中包括一个由CPU_OR合并逻辑错误导致的线程分配异常，属于严重的BUG，强烈建议更新
- 主程序：修复1.2.8部分机型线程绑定异常的问题
- 主程序：修复应用退出又马上进入时核心绑定异常
- 主程序：重写snprintf与strncpy相关代码，换为性能更好的memcpy和指针运算，性能约提升5倍
- 主程序：运行逻辑的进一步完善
- 主程序：支持了一些输入参数：
- '-c ：指定配置文件路径'
- '-h：显示AppOpt的使用说明'
- '-s：指定程序休眠间隔时间，通常不用去调这个参数保持默认就好'
- '-v：显示AppOpt版本'
- 模块：刷入提示与默认applist.conf规则上的小改动

