### v1.3.18更新日志
- 主程序：重构AppOpt为多线程架构
- 主程序：新增"ConfigLoader"线程，负责配置加载
- 主程序：改用inotify实时监控配置文件变化，从轮询改为事件驱动，空闲0占用
- 主程序：当设备不支持inotify或监控失败，则自动回退到轮询机制
- 主程序：实现了主线程/加载器线程通过原子变量同步
- 主程序：修复上一版本(1.3.16)内存泄露问题

### v1.3.5更新日志
- 主程序：性能优化，核心绑定时间更快的同时，程序自身开销更低
- 主程序：修复了一些BUG，其中包括一个由CPU_OR合并逻辑错误导致的线程分配异常，属于严重的BUG，强烈建议更新
- 主程序：修复1.2.8部分机型线程绑定异常的问题
- 主程序：修复应用退出又马上进入时核心绑定异常
- 主程序：重写snprintf与strncpy相关代码，换为性能更好的memcpy和指针运算，性能约提升5倍
- 主程序：运行逻辑的进一步完善
- 模块：刷入提示与默认applist.conf规则上的小改动

### v1.1.9更新日志
- 主程序：修复一个可能导致内存泄露的BUG
- 主程序：支持了一些输入参数：
- '-c ：指定配置文件路径'
- '-h：显示AppOpt的使用说明'
- '-s：指定程序休眠间隔时间，通常不用去调这个参数保持默认就好'

